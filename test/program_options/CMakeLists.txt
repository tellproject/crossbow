file(GLOB files *.cpp)
foreach(f ${files})
    GET_FILENAME_COMPONENT(fname ${f} NAME_WE)
    add_executable(${fname} ${f})
    target_include_directories(${fname} PRIVATE ${Crossbow_INCLUDE_DIRS})
    add_test("${fname}_test" ${fname})
endforeach()
